#include <stdio.h>//—≠ª∑ ‰»Î∑®

void fill(int n)
{
    for (int i = 0; i < n; i++)
    {
        arr[0][i] = t++;
    }
    for (int i = 1; i < n; i++)
    {
    }
}

int main()
{
    int n, t;
    while (scanf("%d", &n) != EOF)
    {
        t = 1;
    }
}
